```python
class Solution:
    def findClosestNumber(self, nums: List[int]) -> int:
        closeVal = nums[0]
        for num in nums[1:]:
            if abs(num) < abs(closeVal) or (abs(num) == abs(closeVal) and num > closeVal):
                closeVal = num
        return closeVal
```  
